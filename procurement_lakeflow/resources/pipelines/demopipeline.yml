resources:
  pipelines:
    pipeline_demopipeline:
      name: demopipeline
      libraries:
        - glob:
            include: ../../src/procurement_lakeflow/demopipeline/transformations/**
      schema: ${var.schema}
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../../src/procurement_lakeflow/demopipeline
